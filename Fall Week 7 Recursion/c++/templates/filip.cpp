#include <iostream>
using namespace std;

int main() {
    // read input
    string a, b;
    cin >> a >> b;

    // reverse the strings
    // hint reverse() reverses a string

    // convert the strings to integers
    // hint stoi() converts a string to an integer
    int valueA = 0;
    int valueB = 0;

    // print the maximum value
    cout << max(valueA, valueB) << endl;
}