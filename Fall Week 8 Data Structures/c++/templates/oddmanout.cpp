#include <vector>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>
#include <list>
#include <stack>
#include <queue>
#include <deque>
#include <utility>
#include <tuple>
#include <string>
#include <iostream>
using namespace std;

//TODO: Find the single number and print (don't do "Case # ...", we do that in main)
void solve(){
    int g;
    cin >> g;

    // TODO: Initialize data structure
    for (int j = 0; j < g; j++){
        int num;
        cin >> num;

        // TODO: Update data structure
    }

    int single;
    /* Iterate through data structure to find single person

    */
    cout << single;
}

int main(){
    int n;
    cin >> n;
    
    for (int i = 1; i <= n; i++){
        cout << "Case #" << i << ": ";
        solve();
        cout << endl;
    }
}